# Server-side only. Never expose this as a VITE_ variable or ship it in the Mini App bundle.
TELEGRAM_BOT_TOKEN=replace_with_a_rotated_token

# Telegram requires a publicly reachable HTTPS URL for production Mini Apps.
MINI_APP_URL=https://example.com
